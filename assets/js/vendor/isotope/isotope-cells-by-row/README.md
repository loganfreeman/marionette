# cellsByRow layout mode for Isotope

See [docs for cellsByRow layout mode](http://isotope.metafizzy.co/beta/layout-modes/cellsbyrow.html)

``` js
$('#container').isotope({
  layoutMode: 'cellsByRow',
  cellsByRow: {
    columnWidth: 200,
    rowHeight: 200
  }
});
```
